spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  application:
    name: service-user
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.11:8848

---

spring:
  profiles: prod
  application:
    name: service-user
  cloud:
    nacos:
      config:
        server-addr: 192.168.1.11:8848