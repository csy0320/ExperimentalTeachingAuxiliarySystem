spring:
  application:
    name: service-manage
  profiles:
    active: dev

---
# 开发环境
spring:
  profiles: dev

  cloud:
    nacos:
      config:
        server-addr: 192.168.1.8:8848
---
# 生产环境
spring:
  profiles: prod

  cloud:
    nacos:
      config:
        server-addr: 192.168.1.11:8848